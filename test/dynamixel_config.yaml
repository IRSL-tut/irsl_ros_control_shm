_default_joint: &default_joint
  DynamixelSettings:
    { Return_Delay_Time: 0, Operating_Mode: 3 }

dynamixel_hardware_shm:
## shm-settings
  ## joint_types: ["", ""]
  ## priority
  ## substeps
  period: 0.02 ## sec(ignored)
## dynamixel-settings
  port_name: /dev/ttyUSB0
  baud_rate: 1000000
  joint:
    # LINK_0
    - { ID: 5, <<: *default_joint}
    # LINK_1
    - { ID: 6, <<: *default_joint}
    # LINK_0
    - { ID: 7, <<: *default_joint}
    # LINK_1
    - { ID: 8, <<: *default_joint}
    # LINK_0
    - { ID: 9, <<: *default_joint}
